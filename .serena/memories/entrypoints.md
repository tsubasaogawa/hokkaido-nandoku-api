# Entrypoints

## API Endpoint

- `GET /random`: Returns a random difficult-to-read place name from Hokkaido.

## Go Application

- The main entry point for the Go application is the `main` function in `source/cmd/api/main.go`. This is the entry point for the AWS Lambda function.
