# Suggested Commands

Here are some of a few useful commands for developing code in this project.

- `cd source && go test ./...`: Run all tests.
- `cd source && go build ./...`: Build the Go application.
- `cd source && go vet ./...`: Run the Go vet linter.
- `cd terraform && terraform plan`: See the changes to be applied to the infrastructure.
