# Commands

All Go-related commands should be run from the `source` directory.

- **Build:** `go build ./...`
- **Test:** `go test ./...`
- **Lint:** `go vet ./...`

Terraform commands should be run from the `terraform` directory.

- **Initialize:** `terraform init`
- **Plan:** `terraform plan`
- **Apply:** `terraform apply`
